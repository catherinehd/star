import { TouchLoadingDirective } from './touch-loading.directive';

describe('TouchLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
