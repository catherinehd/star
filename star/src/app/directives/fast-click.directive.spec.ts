import { FastClickDirective } from './fast-click.directive';

describe('FastClickDirective', () => {
  it('should create an instance', () => {
    const directive = new FastClickDirective();
    expect(directive).toBeTruthy();
  });
});
