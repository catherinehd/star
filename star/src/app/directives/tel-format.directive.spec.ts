import { TelFormatDirective } from './tel-format.directive';

describe('TelFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new TelFormatDirective();
    expect(directive).toBeTruthy();
  });
});
